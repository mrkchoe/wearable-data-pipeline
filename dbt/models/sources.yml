version: 2

# Staging tables landed from S3 (see ingestion.load_s3_to_staging or ingestion.ingest).
sources:
  - name: staging
    schema: staging
    tables:
      - name: daily_activity
        description: "Daily activity CSV data landed in Postgres."
      - name: sleep
        description: "Sleep CSV data landed in Postgres."
